import React, { Component, PropTypes } from 'react';

class ${NAME} extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

${NAME}.propTypes = {};
${NAME}.defaultProps = {};

export default ${NAME};
