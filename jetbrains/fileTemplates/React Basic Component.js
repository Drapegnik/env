import React, { Component, PropTypes } from 'react';

class ${NAME} extends Component {
  propTypes = {};
  defaultProps = {};
  
  render() {
    return (
      <div></div>
    );
  }
}

export default ${NAME};
